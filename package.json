{
  "name": "cli-ts",
  "version": "0.0.1",
  "description": "",
  "type": "module",
  "types": "./dist/index.d.ts",
  "main": "./dist/index.js",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json && esbuild src/index.ts --bundle --platform=node --format=esm --outdir=dist --sourcemap",
    "test": "vitest run",
    "coverage": "vitest run --coverage",
    "fix": "biome check --write"
  },
  "keywords": [],
  "author": "",
  "license": "Apache-2.0",
  "devDependencies": {
    "@biomejs/biome": "2.4.4",
    "@types/node": "^25.3.0",
    "@vitest/coverage-v8": "^4.0.18",
    "esbuild": "0.27.3",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
